-- This migration is a no-op baseline.
-- All tables were already created by the Flask/SQLAlchemy migration that ran first.
-- The actual schema change (adding `status` to bottle_deposits) is in the next migration.
SELECT 1;
